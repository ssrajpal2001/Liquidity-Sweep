# --- Phase 0/1: config, auth, REST connectivity ---
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.31

# --- Later phases (uncomment as each phase is built) ---
# pandas>=2.2               # Phase 3-4: candle/indicator handling
# numpy>=1.26                # Phase 4: ATR / displacement math
# APScheduler>=3.10           # Phase 6: option-chain cache refresh scheduling

# --- Dev/test ---
pytest>=8.0

# --- Web UI: credential vault + broker connect toggle ---
flask>=3.0
cryptography>=42.0
werkzeug>=3.0

# --- Broker SDKs ---
fyers-apiv3>=3.1.3
smartapi-python>=1.4.0   # AngelOne
pyotp>=2.9
logzero>=1.7
pycryptodome>=3.20
